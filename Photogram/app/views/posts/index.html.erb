<h1>Photogram</h1>

<% @posts.each do |post| %>
  <div class="row">
    <%= link_to (image_tag post.image.url(:medium)), post_path(post) %>
    
  </div>
  <p>
      <%= post.caption %>
    </p>
<% end %>